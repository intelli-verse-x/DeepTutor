# Block 1: LLM Factory 探测

探测 DeepTutor 的 LLM Factory 接口，输出签名和用法给后续 Block 使用。

## 操作

1. 读取 `deeptutor/services/llm/factory.py`，理解 LLMFactory 的：
   - 实例化方式（单例？每次 new？构造函数参数？）
   - 主要调用方法（`chat()`, `complete()`, `generate()`?）
   - 方法签名（参数类型、返回值类型）
   - 如何传入 system prompt 和 user message

2. 读取 `deeptutor/services/llm/__init__.py`，看公开导出了什么

3. 确认 import 路径和最小调用示例

## 输出要求

在项目根目录创建 `.hermes_llm_factory_api.md`，写入：

```markdown
# LLM Factory API

## Import
```python
from deeptutor.services.llm.factory import <正确的类名>
```

## Instantiation
```python
<实例化代码>
```

## Call Method
```python
<调用代码，包含 system prompt 和 user message>
```

## Return Type
<返回值类型和格式>
```

## 验收

```bash
# 验证 .hermes_llm_factory_api.md 存在且包含必要信息
python -c "
import os
path = r'D:\Project\DeepTutor\.hermes_llm_factory_api.md'
assert os.path.exists(path), 'File not found'
content = open(path).read()
assert 'Import' in content
assert 'Instantiation' in content
assert 'Call Method' in content
print('BLOCK 1 COMPLETE')
"
```
